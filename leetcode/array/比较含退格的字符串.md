# 每日学习记录

## 📅 2026-05-03（第0周）

### 今天学了什么
- 思路上  采用双指针 一个在s上走  一个在t上走   都从后面开始走  再用一个skip记录中间遇到的#数量  如果遇到有效字符 skip==0 那就是真正有效字符 如果skip>0 那就让skip-1 继续走 直到遇到真正有效字符或者走完   把两者的真正有效字符比较 如果不相等 直接返回false 如果相等 继续寻找下一个  如果一个走完了 另一个还有有效字符 那就是false

学到的东西还有这段代码 ：
if (i == -1 && j == -1)
	return true;
else if (i == -1 || j == -1)
	return false;
else if(s[i]!=t[j])
	return false;
else {
	i--;
	j--;
}      
这里：
else if (i == -1 || j == -1)
	return false;  我可能还会写 if (i == -1 && j >0||i>0&&j==-1) 但实际上上面就没有同时是-1的情况了  之后一旦出现一个-1 另一个肯定不是  

### AI 使用记录


### 遇到的问题
没有思路 
思路中 如果相等 继续寻找下一个 代码  
else if(s[i]!=t[j])
	return false;
else {
	i--;
	j--;
}
  这里的
  else {
	i--;
	j--;
}  忘写了 以为在函数中就执行了  实际上要手动执行

### 怎么解决的
查AI解决  

### 明天要做什么
